const fs=require('fs'); p='c:/rehyntrixai/app/credits-billing/page.tsx'; c=fs.readFileSync(p,'utf8'); c=c.replace(/  useEffect\(\) \{[\s\S]*?\n  \}\s*,\r?\n  \s*\[user, initialized\]\)[\s\S]*?fetchBillingData\(\)/,'  useEffect(() =    if (!initialized) return\n    if (!user) {\n      router.push(\'/auth/login\')\n      return\n    }\n    fetchBillingData()\n  }, [user, initialized, supabaseClient])\n\n  async function fetchBillingData'); fs.writeFileSync(p,c); console.log('done') 
